/**
 * Message bundles were permanently retired in favor of EverCoin.
 *
 * This intentionally exports nothing and reads no environment variables.
 * The file remains as a harmless replacement so direct GitHub ZIP uploads can
 * remove the old implementation without requiring a separate delete action.
 */
export {};
