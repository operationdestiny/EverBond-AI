# EverBond v40 — Ready Localhost Site

This version replaces the hero banner with a full 2048x514 asset and displays it at that same ratio.

Run:

```powershell
npm install
npm run dev
```

Open:

```text
http://localhost:3000
```
